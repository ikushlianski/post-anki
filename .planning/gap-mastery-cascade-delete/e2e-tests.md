# E2E test coverage — gap_mastery cascade delete

Generated by /write-playwright-tests from scenarios.md. Status: RED (pre-implementation).

**Coverage type: backend integration test, not e2e.** This ticket has zero UI-observable surface —
every reader of `gap_mastery` joins through `gaps` (`listMasteryTrackedGapsAcrossSubjects` uses an
INNER JOIN), so a Playwright browser test cannot detect an orphaned `gap_mastery` row in either
direction: the screen renders identically whether the leak exists or not. scenarios.md's
"Proof-mechanism note" documents this explicitly and points to the same precedent already used for
`.planning/generalize-gap-tracking/scenarios.md` SCENARIO 8
(`apps/api/src/probe-session/gap-mastery-concurrency.integration.test.ts`). The table below uses
"Drives" instead of "Clicks" and "Asserts — DB" for both layers, since there is no UI layer to
assert against.

| Scenario | Test (tag) | Drives (direct function call, no browser) | Asserts — before | Asserts — after (DB) | Status |
|---|---|---|---|---|---|
| S1 — deleting a gap's topic, module, or curriculum also deletes its `gap_mastery` row | n/a (integration test, no `@TICKET.S<N>` tag — see scenarios.md's note on why) | `deleteTopic(topicId)` | seeded `gap_mastery` row exists (count = 1) | `SELECT * FROM gap_mastery WHERE gap_id = $1` returns 0 rows | RED — fix (`deleteGapMasteryForGapIds`) not implemented yet |
| S1 (module case) | same file, 2nd `it` | `deleteModule(moduleId)` | count = 1 | 0 rows | RED |
| S1 (bulk case) | same file, 3rd `it` | `deleteModules([moduleIdA, moduleIdB])` | both counts = 1 | 0 rows for BOTH gap ids | RED |
| S1 (curriculum case) | same file, 4th `it` | `deleteCurriculum(curriculumId)` (exercises `clearCurriculumStructure`) | count = 1 | 0 rows | RED |

## Scenario → test mapping

- S1 (all four call sites) → `apps/api/src/gap/gap-mastery-cascade-delete.integration.test.ts`
  - `it` 1: `deleteTopic`
  - `it` 2: `deleteModule`
  - `it` 3: `deleteModules` (bulk, two modules)
  - `it` 4: `clearCurriculumStructure` via `deleteCurriculum`

## Why this is not a `features/<feature>/tests/*.test.ts` Playwright file

- No `features/<feature>` folder applies (`playwright.md`'s "Target" section: `Feature: none`).
- No action, no `data-testid`, no dev-server URL is used or needed — the test talks to Postgres
  directly via `pg.Client`, the same harness shape as the concurrency-test precedent.
- Because this is integration-only, no `features/<feature>/tests/README.md` co-located index was
  written either — that index only exists for features with a Playwright `tests/` folder, and this
  ticket has none.

## Actual RED run (confirmed against real local Postgres)

Command: `cd apps/api && npm run test:integration -- src/gap/gap-mastery-cascade-delete.integration.test.ts`
Target DB: `postanki_e2e`, docker container `post-anki-e2e-db`, port 5436 (already running locally).

```
 ❯ src/gap/gap-mastery-cascade-delete.integration.test.ts (4 tests | 4 failed) 49ms
   × deleteTopic ... → expected 1 to be +0 // Object.is equality
   × deleteModule ... → expected 1 to be +0 // Object.is equality
   × deleteModules (bulk) ... → expected 1 to be +0 // Object.is equality
   × clearCurriculumStructure (via deleteCurriculum) ... → expected 1 to be +0 // Object.is equality

 Test Files  1 failed (1)
      Tests  4 failed (4)
```

Each case's pre-deletion assertion (`gap_mastery` count = 1, proving the seed landed) passes; only
the post-deletion assertion fails, confirming the test is red for the intended reason — the
`gap_mastery` row survives deletion today — and not for an infrastructure or seeding defect.

## Action gaps this run created (for /implement-playwright to fill)

None — no Playwright action catalog applies to this ticket.

## What is NOT covered yet (flag for the human)

Nothing outstanding. `scenarios.md` has exactly one scenario and it is fully covered by this
integration test, per the plan's own explicit proof-mechanism note.
