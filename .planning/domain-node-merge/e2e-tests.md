# E2E test coverage — domain-node-merge

Generated by /write-playwright-tests from scenarios.md. Status: RED (pre-implementation).

| Scenario | Test (tag) | Clicks / drives | Asserts — UI | Asserts — persistence | Status |
|---|---|---|---|---|---|
| S1 — merge two domain nodes with real children, none orphaned or duplicated | `@domain-node-merge.S1` (`merge-domain-node-reassigns-curriculum-and-child`) | seed target A, an unrelated parent, source B (child of the unrelated parent), child C (child of B); create a real curriculum under B via "add course here"; open B's merge control, pick A from the path-labeled `<select>`, confirm | source B's tile removed from DOM; target A's subtree renders the moved curriculum link and the moved child node C | `domain_nodes` row B deleted; `domain_nodes` row C's `parent_id` = A; `curricula` row's `domain_node_id` = A; `GET /subjects/:id/domain-map` (the tree-assembly read path) nests C directly under A | RED (endpoint, repo fn, and merge-button testids don't exist yet) |
| S2 — merge-target picker excludes invalid targets | `@domain-node-merge.S2` (`merge-picker-excludes-invalid-targets`) | seed a node with a child + grandchild (its own subtree), a sibling, and an unrelated-branch node two levels deep; open the node's merge picker, read back the `<select>` options | picker option list never includes the node itself or any node in its own subtree; includes the sibling and the unrelated-branch node labeled with its full path (`Tools > CLI`) | none (read-only; the picker is opened and read, never confirmed) | RED (merge-button/select testids don't exist yet) |
| S3 — deliberately malformed merge is rejected, not corrupted (not e2e) | integration test only | build a real 3-level chain B → C → A (B is A's grandparent), call `mergeDomainNodes(A, B)` directly; also a `CHAIN_DEPTH = 9` deep-chain case; also the negative-control "merge into direct parent" case | n/a — no UI surface, bypasses the picker entirely by design | `{ error: "cycle" }` for both malformed cases, zero writes (tree byte-for-byte unchanged); the direct-parent case succeeds normally | RED (`mergeDomainNodes` doesn't exist yet — `apps/api/src/domain-map/domain-node-merge-cycle-guard.integration.test.ts`) |
| S4 — two concurrent merges racing for the same source node (not e2e) | integration test only | two concurrent `mergeDomainNodes` calls for the same source node into two different targets, fired via `Promise.all` | n/a | exactly one succeeds with real moved counts, the other resolves `{ error: "not_found" }` (never throws); source's children/curricula end up on the winner only | RED (`apps/api/src/domain-map/domain-node-merge-concurrency.integration.test.ts`) |
| S5 — `domainNodeProgress()` rollup stays correct against a merged tree shape (not e2e) | unit test only | construct a merged-shape tree directly (target with pre-existing child + reparented children including a same-named pair), attach known topic maturities | n/a | `domainNodeProgress()` returns the correct averaged percent for the target's subtree, not truncated by the deriver's own `MAX_DEPTH = 6` cap | GREEN already — this test only exercises the existing, unmodified `domainNodeProgress()`, so it needed no new implementation (`packages/core/src/domain-map/domain-map-progress.test.ts`) |

## Scenario → test mapping

- S1 → `verification-repo/projects/post-anki/post-anki/features/domain-map/tests/merge-domain-node-reassigns-curriculum-and-child.test.ts`
- S2 → `verification-repo/projects/post-anki/post-anki/features/domain-map/tests/merge-picker-excludes-invalid-targets.test.ts`
- S3 → `apps/api/src/domain-map/domain-node-merge-cycle-guard.integration.test.ts`
- S4 → `apps/api/src/domain-map/domain-node-merge-concurrency.integration.test.ts` (also carries S1's own zero-orphan + read-path proof, per spec.md's Definition of Done allowing it co-located in this file)
- S5 → `packages/core/src/domain-map/domain-map-progress.test.ts` (`isAncestor` unit coverage + the merged-shape rollup test)

## Action gaps this run created (for /implement-playwright to fill)

- `mergeDomainNode` / `openDomainNodeMergePicker` — new, `features/domain-map/actions/merge-domain-node.action.ts`. Both drive real testids that don't exist yet on `apps/web/src/domain-map/domain-map-tree.tsx`.
- New testids required on the source app (per `playwright.md`):
  - `domain-map-node-merge-button-<nodeId>`
  - `domain-map-node-merge-target-select-<nodeId>` (options labeled by full path)
  - `domain-map-node-merge-confirm-<nodeId>`
  - `domain-map-node-merge-cancel-<nodeId>`
- Backend: `POST /domain-nodes/:targetId/merge`, `mergeDomainNodes()` in `apps/api/src/domain-map/domain-map.repo.ts`, `handleMergeDomainNodes` in the controller, route + `RouteName` entry, `server.ts` import + switch case, `mergeDomainNodesInput`/`mergeDomainNodesResultSchema` in `packages/shared/src/domain-map.ts`.
- Core: `isAncestor()` in `packages/core/src/domain-map/domain-map-progress.ts`.
- Frontend: `MergeDomainNodeButton` in `apps/web/src/domain-map/merge-domain-node-button.tsx`; `domain-map-tree.tsx` threads an `allNodes` prop and renders the button; `mergeDomainNodes` server fn in `apps/web/src/domain-map/domain-map.api.ts`; `mergeDomainNodes` HTTP client fn in `apps/web/src/curriculum/api-client.ts`.

## What is NOT covered yet (flag for the human)

- Nothing — every scenario in `scenarios.md` has a test (e2e for S1/S2, integration for S3/S4, unit for S5). S3/S4/S5 are deliberately not e2e per `playwright.md`'s own routing (malformed-merge and concurrency races need real-Postgres direct calls; the rollup math is pure logic) — this mirrors `ontology-split-merge`'s own precedent for the same class of proof.
