# E2E test coverage — doc-changelog-scan (issue #49)

Generated by `/write-playwright-tests` from `scenarios.md`. Status: GREEN — implemented and verified
in the same session (this ticket's implementation phase ran inline, not as a separate handoff).

| Scenario | Test (tag) | Clicks / drives | Asserts — UI | Asserts — persistence | Status |
|---|---|---|---|---|---|
| S1 — content hashing detects change/non-change | `tracked-tool-fetcher.test.ts` (vitest, no e2e) | n/a (pure logic) | n/a | identical content → identical hash; 1-char diff → different hash; fetch failure → null; mocked-content-set → zero real fetch calls, incl. when a tool_key is missing from the mock map | GREEN |
| S2 — first-ever scan, exactly 1 agent call | `doc-scan.orchestrator.test.ts` (vitest, no e2e) | n/a (orchestrator-level) | n/a | 1 agent call w/ all 4 tools' content; 4 watermark rows inserted; 1 topic + 1 supersession row | GREEN |
| S3 — the "never a firehose" proof | `doc-scan.orchestrator.test.ts` (vitest, no e2e) | n/a | n/a | 2nd run w/ identical content → 0 agent calls (call-count assertion on the mock, not the orchestrator's own flag), 0 new rows | GREEN |
| S4 — only changed tools reach the prompt | `doc-scan.orchestrator.test.ts` (vitest, no e2e) | n/a | n/a | 1 agent call; prompt contains only the 1 changed tool's content, excludes the other 3 | GREEN |
| S5 — Scan now surfaces both kinds | `@doc-changelog-scan.S5` (trigger-doc-scan-surfaces-both-suggestion-kinds) | Scan now | New topics found / Possibly outdated populate w/ reason + doc-scan label | 1 topic row + 1 supersession row, both `source: doc-scan`, `status: pending` | GREEN |
| S6 — accept new-topic creates node | `@doc-changelog-scan.S6` (accept-new-topic-suggestion-creates-node) | Accept (new topic) | suggestion leaves pending list; new node visible under correct parent on the map | new `domain_nodes` row under `proposed_parent_node_id`; suggestion `accepted`, `created_domain_node_id` set | GREEN |
| S7 — reject new-topic creates no node | `@doc-changelog-scan.S7` (reject-new-topic-suggestion-creates-no-node) | Reject (new topic) | suggestion leaves pending list | `domain_nodes` row count unchanged; suggestion `rejected`, not deleted | GREEN |
| S8 — accept supersession flags, percent unchanged | `@doc-changelog-scan.S8` (accept-supersession-suggestion-flags-without-touching-percent) | Accept (supersession) | "possibly outdated" badge appears beside the unchanged percent badge | `superseded_at`/`superseded_reason` set; percent byte-identical before/after (also proven at the backend layer in `doc-scan.orchestrator.test.ts`) | GREEN |
| S9 — reject supersession leaves unflagged | `@doc-changelog-scan.S9` (reject-supersession-suggestion-leaves-node-unflagged) | Reject (supersession) | no badge appears | `superseded_at` stays null; suggestion `rejected`, not deleted | GREEN |
| S10 — agent failure leaves watermark un-advanced | `doc-scan.orchestrator.test.ts` (vitest, no e2e) | n/a | n/a | no throw; 0 new rows; the 2 changed tools' `last_content_hash` unchanged from before the failed call (captured pre-call, compared with `toBe`); `log.error` called | GREEN |

Backend DoD proofs with no dedicated `SCENARIO N` number (spec.md's Backend DoD, not
`scenarios.md`) — all homed in `doc-scan.orchestrator.test.ts` alongside S2/3/4/10:
- `MAX_TOTAL_SUGGESTIONS` cap: 6 raw resolvable suggestions (3+3, schema max) → exactly 3 topic
  rows + 2 supersession rows (the split spec.md's step 5b pins, not merely a sum of 5).
- `POST /subjects/:id/doc-scans` endpoint-level proof (via `handleTriggerDocScan`, not the bare
  orchestrator function): exactly 1 topic row + 1 supersession row, `200` response.
- SCENARIO 10's own Integration line ("still responds 200, never 502") proven through
  `handleTriggerDocScan` directly, not only through the bare `runDocScan()` return value.
- `handleTriggerAllDocScans` (the scheduled job's actual entry point, `POST /doc-scans` → 
  `runDocScanForAllTrackedSubjects()` → `listSubjectIdsWithDomainNodes()`) — proven to dispatch
  across every gated subject and return 200; this test is also what surfaced and deterministically
  demonstrated the cross-subject watermark limitation documented in `todo.md` and
  `docs/architecture/doc-changelog-scan.md` — fixed 2026-08-01 by the `(subject_id, tool_key)`
  composite key (migration `0030`), so the test now asserts that EVERY gated subject gets a real
  agent call rather than exactly one.
- `PATCH /domain-topic-suggestions/:id` accept/reject, at the controller layer.
- `PATCH /domain-supersession-suggestions/:id` accept/reject, at the controller layer, including
  the percent-byte-identical negative assertion against a real attached, studied curriculum.

## Scenario → test mapping

- S1 → `apps/api/src/domain-map/tracked-tool-fetcher.test.ts`
- S2, S3, S4, S10 + the 4 unnumbered backend proofs above →
  `apps/api/src/domain-map/doc-scan.orchestrator.test.ts`
- S5 → `verification-repo/projects/post-anki/post-anki/features/domain-map/tests/trigger-doc-scan-surfaces-both-suggestion-kinds.test.ts`
- S6 → `.../accept-new-topic-suggestion-creates-node.test.ts`
- S7 → `.../reject-new-topic-suggestion-creates-no-node.test.ts`
- S8 → `.../accept-supersession-suggestion-flags-without-touching-percent.test.ts`
- S9 → `.../reject-supersession-suggestion-leaves-node-unflagged.test.ts`

## Action gaps this run created (and filled)

- `triggerDocScan` (`features/domain-map/actions/trigger-doc-scan.action.ts`) — implemented, not
  stubbed; the whole feature landed in this session.
- `resolveDocScanSuggestion` (`features/domain-map/actions/resolve-doc-scan-suggestion.action.ts`)
  — implemented, not stubbed.
- New seeds: `seedPendingDomainTopicSuggestion`, `seedPendingDomainSupersessionSuggestion`.
- New mock-openrouter responder: `doc-scan` (matches `schemaProps.includes('newTopicSuggestions')`,
  confirmed non-colliding against every existing responder's schema keys).
- New env wiring: `E2E_MOCK_TRACKED_TOOL_CONTENT`, set once in
  `verification-repo/playwright.post-anki.config.ts`'s api `webServer` env block (static fixture
  content for all 4 real `TRACKED_TOOLS` keys — S5 is the only scenario that triggers a live scan).
- New db test helper: `deleteRows()` in `verification-repo/.../db/pg.ts` — needed because
  `tracked_tool_scan_state` is a GLOBAL table (keyed only by `tool_key`, not per-subject), unlike
  every other table this suite touches; S5 explicitly clears it as a precondition so the test is
  correct on a repeat run against the same persistent local e2e Postgres, not just a pristine one.

## What is NOT covered yet (flag for the human)

- None. Every scenario in `scenarios.md` (S1-S10) has a passing test, and every backend DoD line
  from `spec.md` without its own scenario number has a proof in `doc-scan.orchestrator.test.ts`.
- The one genuine gap is infrastructure-layer, not test-layer: the actual `pulumi up` deploying the
  new Cloud Scheduler job could not run in this session (missing `PROD_PULUMI_CONFIG_PASSPHRASE`) —
  see `todo.md` for the exact manual steps and the session report for the `pulumi preview` attempt.
