# E2E test coverage — ontology-audit-trail Merge/split audit trail

Generated by /write-playwright-tests from scenarios.md. Status: RED (pre-implementation).

| Scenario | Test (tag) | Clicks / drives | Asserts — UI | Asserts — persistence | Status |
|---|---|---|---|---|---|
| S1 — merging two subjects via the UI writes a visible, correctly-populated log row | @ontology-audit-trail.S1 (admin-observability) | Create Subject ×2 → create curriculum under source → seed source domain node (back door) → click source's merge button → pick target → confirm → open /admin-observability | new row shows entity `subject`, correct target/source names, `curriculaMoved`/`domainNodesMoved` counts in the reassigned-text cell | one `ontology_merges` row (`entity_type='subject'`), `reassigned_counts` cross-checked against the real post-merge `curricula`/`domain_nodes` row counts under the target | RED (table `ontology_merges` + testids + `ontology-merge.repo.ts` don't exist yet) |
| S2 — mergeTags writes a correct audit log row (not e2e) | backend only | n/a | n/a | one `ontology_merges` row (`entity_type='tag'`), `reassigned_counts` matches `mergeTags`' own returned `assignmentsMoved`/`assignmentsDeduped`/`sessionsMoved` exactly | RED (module `ontology-merge.repo.ts` doesn't exist) |
| S3 — mergeCurricula writes a correct audit log row + negative case (not e2e) | backend only | n/a | n/a | one `ontology_merges` row (`entity_type='curriculum'`), all 5 `reassigned_counts` fields exact; `target_failed` rejection writes **zero** rows | RED (module doesn't exist) |
| S4 — mergeDomainNodes writes a correct audit log row (not e2e) | backend only | n/a | n/a | one `ontology_merges` row (`entity_type='domain_node'`), both `reassigned_counts` fields exact | RED (module doesn't exist) |
| S5 — the admin view renders all four entity types correctly, independent of S1–S4 | @ontology-audit-trail.S5 (admin-observability) | Seed 4 `ontology_merges` rows (one per entity type, back door) → open /admin-observability | all 4 rows render with correct entity label, target/source names, and each entity type's own reassigned-counts field names | none beyond the 4 seed inserts | RED (table + testids don't exist yet) |

Extra (not in scenarios.md, added per this run's own instructions): a real-Postgres proof that the log write runs inside each merge's own transaction — a simulated failure in the log insert rolls back the merge's reassignment too, not just the log row. `apps/api/src/ontology-merge/ontology-merge-log-rollback.integration.test.ts`. RED for the expected reason today (mergeTags doesn't call `insertOntologyMergeLog` yet, so the mocked failure is never hit and the merge just succeeds normally — the test correctly reports "promise resolved instead of rejecting").

## Scenario → test mapping

- S1 → `verification-repo/projects/post-anki/post-anki/features/admin-observability/tests/subject-merge-writes-log-row.test.ts`
- S2/S3/S4 → `apps/api/src/ontology-merge/ontology-merge-log.integration.test.ts`
- S5 → `verification-repo/projects/post-anki/post-anki/features/admin-observability/tests/renders-all-four-merge-entity-types.test.ts`
- (extra) transactional-rollback proof → `apps/api/src/ontology-merge/ontology-merge-log-rollback.integration.test.ts`

## Action gaps this run created (for /implement-playwright to fill)

- `openAdminObservabilityPage` — new, `features/admin-observability/actions/open-admin-observability.action.ts`. Written for real (not a throwing stub) since the testid contract was already fully specified in `playwright.md`; will fail with `ActionFailure.missingTestId` until `apps/web/src/routes/admin-observability.tsx` adds the `admin-observability-merges-table` testid.
- `readOntologyMergeRows` — new, same folder. Same reasoning — reads the per-row/per-cell testids (`admin-observability-merge-row`, `-entity-type`, `-target-name`, `-source-name`, `-reassigned`), all still missing from the page today.
- `insertOntologyMergeLogRow` — new seed, `features/admin-observability/seeds/seed-ontology-merge-fixture.ts`. Direct SQL insert into `ontology_merges` — will fail until the table exists (migration).

## What is NOT covered yet (flag for the human)

Nothing — every scenario in `scenarios.md` (S1–S5) has either an e2e test (S1, S5) or a named backend-integration case (S2, S3, S4), matching the plan's own e2e/integration split exactly. No gap to route back to `/plan-playwright`.
