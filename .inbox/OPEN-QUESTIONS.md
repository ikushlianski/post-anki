# Open Questions

## mobile (iPhone 13 / 390px) responsive pass (2026-06-01)
- [x] DONE: mobile-first Tailwind pass for the 390px viewport (below the `sm` 640px breakpoint, so base classes = mobile). Nav `flex-wrap` + `px-5 sm:px-8` (6 items wrap instead of overflowing); all route `main`s `px-5 py-8 sm:px-8 sm:py-10`; dashboard tree nodes `min-w-0 flex-1 truncate` so long titles don't shove the status dropdown off-screen; module/topic/subject headers `flex-wrap` + `min-w-0`; `TopicShapeBar` wraps; `ConfirmBar` stacks (`flex-col sm:flex-row`); curriculum list names + probe-panel header truncate; `StatusBadge` `shrink-0`. Verified: all 6 routes SSR 200 against live API+DB, responsive classes present in served HTML, viewport meta present, typecheck clean. (No browser MCP this session → verified via SSR HTML + layout reasoning, not pixel screenshots.)
- [x] DB DRIFT — RESOLVED (BE 2026-06-01): the transient `column "speed" does not exist` was the window before the BE migration landed mid-session. Verified the ledger DOES exist at `drizzle.drizzle_migrations_api` (drizzle's own schema, not public — that's why a bare `\dt` / unqualified select misses it) with **2 migrations applied** (`0000` initial + `0001` adaptive cols). `db:migrate` is the proper path and recorded both; no push-drift remains. Columns `speed`/`hinting`/`default_depth` live with defaults normal/true/working.

## adaptive settings + probe grounding (BE, 2026-06-01)
- [x] BUILT + live-verified: **adaptive settings** on the curriculum — `speed` (slow|normal|fast), `hinting` (bool), `defaultDepth` (awareness|working|deep). Migration `0001`. Settable via `PATCH /curricula/:id` (now accepts any of learningStatus/speed/hinting/defaultDepth, all optional). speed = probing pace/difficulty ramp, hinting = mentor adds a one-line hint — both wired into the mentor-ask prompt. Verified: defaults → PATCH(fast,false,deep) persists → bad enum 400.
- [x] BUILT + live-verified: **probe-time grounding** (Principle 1 relaxed, sources-first → web fallback). Curriculum with pasted TEXT sources (≥200 chars) → mentor grounds in them, no web. Otherwise → web-search (openrouter:web_search), never raw training data. Verified: source-backed → `source=pasted`, 0 web calls; source-less → `source=web` (2731 chars, ~21s).
  - **✅ FE DONE + live-verified (2026-06-02):** FE `Curriculum` gained `speed`/`hinting`/`defaultDepth` (model + `mapCurriculum`, defaultDepth depth-mapped). `updateCurriculumSettings` api-client + BFF (`PATCH /curricula/:id`). New **`AdaptiveSettings` panel** on the curriculum detail page (collapsible, shown when ready|confirmed): Speed 3-segment (Slow/Normal/Fast), Hinting on/off toggle, Default-depth **3-level dragging slider**. New reusable **`DepthSlider`** (range input + clickable labels) also **replaced the per-topic depth dial** in `topic-row.tsx` (the "depth as a slider" ask). Verified live via `verify-adaptive.ts`: reads mapped fields → PATCH(fast/off/deep) returns+persists → restored. Web typecheck clean.
  - **✅ BE BLOCKER RESOLVED (2026-06-02):** `fetched_text` migration `0002_old_echo.sql` generated + applied; `GET /curricula/:id` works again. (It was mid-implementation of "persist fetched source text at capture".)

## three BE changes: source-text persistence, answerable daily-push, gaps-inline (2026-06-02)
- [x] **Persist fetched source text at capture** — `sources.fetched_text` column (migration `0002`). At parse/merge the orchestrator resolves each source (text→value, **link→fetched+stripped**) and stores it; probe grounding now reads stored `fetched_text` for ALL source kinds. So a **link/article you paste is used to ground probing** (before, link bodies were discarded and link-only curricula fell to web). Verified live: Wikipedia link → `source=pasted 20000 chars`.
- [x] **Answerable daily-push** — `GET /daily-push?mode=socratic|quick_test` now returns `{ push, question }`: a ready probe question for the selected gap (reuses the mentor + grounding pipeline). Answer via the existing `POST /topics/:id/probe/answer` with `{ gapId: question.gapId, mode, answer }`. This makes the daily loop drivable end-to-end by any client (web **or Telegram bot**). Verified: socratic returns gap-anchored question; quick_test returns 4 options.
- [x] **Gaps inline in curriculum detail** — `GET /curricula/:id` topics now include a `gaps` array (no more N×M per-topic gap fetches for the detail view). `topicSchema.gaps` is optional (only the detail endpoint populates it). Verified: declared gap shows inline.
- [x] **Response consistency** — `POST /curricula/:id/sources` and `/reparse` now return a **full `Curriculum`** (was `{id,name,status}`).
  - **⚠️ FE (additive contract changes):** `topicSchema.gaps?: Gap[]` (use it for the detail view instead of per-topic fetches); `dailyPushResponseSchema` += `question: ProbeQuestion|null` (render it on `/today`, and `?mode=` selectable); `/sources` + `/reparse` now return full `Curriculum`.
- **Telegram-as-client:** the API is already client-agnostic (HTTP + `API_SHARED_SECRET` bearer, single-owner, no web coupling). A Telegram bot drives the same endpoints: `GET /daily-push` → send question → user replies → `POST /topics/:id/probe/answer`. The only missing piece for actual TG delivery is the **bot app rejoin** (apps/bot, Mastra 1.x) + a scheduled trigger — separate from the API.
  - **Known limits (flagged):** grounding uses pasted TEXT sources only (`link` bodies aren't retained from capture → link-only curricula fall to web; storing fetched text at capture is the future fix); web fallback runs per probe action on source-less curricula (~21s + ~$0.005); coverage is presence-based, not per-gap.

## capture + shape / probe cold-start (2026-06-01)
- [x] BUILT + live-verified: capture is paste-first (modules + OPTIONAL topics, no web, no gaps — gaps are user-only/Socratic-discovered per DECISIONS). Manual shaping endpoints: add/rename/reorder/delete modules & topics, move topic between modules, bulk reorder. Verified against real Neon+OpenRouter.
- [x] FE SHAPE EDITOR BUILT + live-verified (2026-06-01): wired every shaping endpoint through the FE. api-client: `createModule`, `updateModule` (rename/order), `deleteModule`, `reorderModules` (`PATCH /curricula/:id/modules/order`), `createTopic` (depth-mapped), `deleteTopic`, `reorderTopics` (`PATCH /modules/:id/topics/order`); `updateTopic` extended with title/summary/moduleId(move)/order. BFF server fns added. UI: curriculum detail page is now an inline editor when status is `ready`|`confirmed` — `shape-controls.tsx` (ReorderButtons ▲▼, InlineRename, ConfirmDelete, AddInline) + `topic-shape-bar.tsx` (reorder/move-to-module/delete per topic); module headers rename+delete+reorder, "+ Add module"/"+ Add topic", topic move via dropdown. Empty ready/confirmed curriculum now shows "add a module" instead of a false parse-failed banner. New input schemas in `model.ts` (createModuleInput/updateModuleInput/createTopicInput/reorderInput). Verified live via `apps/web/scripts/verify-shape.ts` (self-cleaning) against real Neon: create→rename→createTopic×2→reorderTopics→re-depth→reorderModules→move-topic→delete all PASS through the real api-client; base `verify-api-client.ts` still green. Web typecheck clean.
- [x] SHAPE GATING decided (user 2026-06-01): editor stays enabled for `ready` AND `confirmed` — restructure anytime; moved/renamed topics keep their gaps by design.
- [x] **PROBE COLD-START — DONE + live-verified (2026-06-01):** `startProbe` on a zero-gap topic now returns a topic-level OPENING question (gapId=null) instead of erroring; answering it makes mentor-eval discover the first real gaps, and the next question auto-anchors on a discovered gap. `no_open_gaps` error removed. learningStatus now derives from remaining open gaps (`reviewing` when none remain, else `probing`). Verified live: 0 gaps → opener → answer → 2 gaps discovered → next question on a real gap.
  - **✅ FE ADAPTED (2026-06-01):** the nullable-gap contract caught the web build at compile time (the value of importing `@post-anki/shared`). FE `Question.gapId/gapLabel` + `recordAttemptInput.gapId` made nullable; `mapProbeQuestion` keys the opener id as `opener:<kind>`; `submitProbe` accepts `gapId: string|null`. UX (per user): opener is **explicitly labelled** ("Opening question — your answer helps me map what you know; I'll turn it into gaps"), gap-anchored questions still show their gap label; the stale "no open gaps" empty message reworded (startProbe always returns a question now). Fresh-topic CTA shows **both** "Poke me" and "declare a gap" with a one-line hint that either fills the checklist. Live-verified via `verify-newcontract.ts`: 0 gaps → opener (gapId null) → answer (gapId:null) → gaps discovered.
- [x] FULL COVERAGE (2026-06-01): wired `DELETE /subjects/:id` (cascade-aware confirm in `SubjectSection`) and `DELETE /curricula/:id` (per-curriculum two-click delete in the subject list). Every product route in `server.ts` now has a FE consumer (only `GET /healthz` is infra). Live harnesses green (`verify-api-client`, `verify-shape`, `verify-newcontract` — the last now also covers create/delete curriculum); web typecheck clean.
- [x] DONE (no mock): `apps/web/src/curriculum/api-client.ts` typed HTTP client for every BE endpoint; BFF (`curriculum.api.ts`, `subject.api.ts`) ALWAYS calls the real API (default `API_BASE_URL=http://localhost:8030`). Mock fallback REMOVED and `stub/store.ts` + `mock-data.ts` DELETED. Typecheck clean. The app now requires the real API — with none running it errors by design (that's "no mock").
- [x] RESOLVED (gap data source): BE added `GET /topics/:id/gaps`. Client now fetches real gaps per topic and populates `topic.gaps` + computes `gapsTotal/gapsCovered` in `getCurriculumDetail` (detail view only). `getTree` calls it with `{ withGaps: false }` so the dashboard does NOT fan out N×M gap fetches; the dashboard node now shows `maturity %` + learningStatus (both real under withGaps:false) instead of gap counts (BE `progress` carries no gap totals, so 0/0 would have been a lie). `listTopicGaps` is fail-soft (returns `[]` on error) so one gap-fetch 500 can't 404 the whole curriculum detail page. `gap.socratic` in the FE model is now vestigial (questions are dynamic at probe time) → mapped to `''`.
- [x] RESOLVED (add-sources): BE added `POST /curricula/:id/sources` (202 curating + background merge). `addSourcesToCurriculum` now calls it instead of throwing; the existing form already refetches via `router.invalidate()`.
- [x] RESOLVED (reparse): `POST /curricula/:id/reparse` wired to a `FailedBanner` "Re-parse sources" button.
- [x] RESOLVED (status enum + async lifecycle): user authorized THIS session to own `model.ts`. Added `confirmed`/`failed` (FE enum now matches BE 1:1, no lossy mapping). Detail page is status-driven: `curating/draft` → self-polling "parsing…" banner (re-invalidates every 2.5s); `failed` (or ready-with-0-modules) → re-parse banner; `ready` → "Confirm curriculum" gate bar; `confirmed` → modules with probing enabled. Probing ("Poke me") is gated — disabled with a hint until `confirmed`, so the BE's `409 not_confirmed` is never hit. `StatusBadge` shows all 5 states with colour.
- [x] RESOLVED (confirm gate): `ConfirmBar` wires `POST /curricula/:id/confirm` (ready→confirmed).
- [x] BUILT (net-new screens): `GET /daily-push` → `/today` card (reason badge + deep-link to probe); `GET /cross-cutting` → `/concerns` page (per-concern coverage bars); `POST /decide` → `/decide` form (strengths/blind-spots/questions/verdict). Nav links added in `__root.tsx`.
- [x] RESOLVED (richer probe result): `POST /topics/:id/probe/answer` returns `nextQuestion`; FE now threads it so "Probe the next gap" advances in-place (no extra round-trip). Removed the fake `remainingGaps: 0`. (BE also returns `progress`/`learningStatus` on answer; FE relies on `router.invalidate()` refetch for those.)
- [x] COVERAGE: every ready BE route now has a FE consumer. Web typecheck clean.
- [x] CONTRACT VERIFIED STATICALLY: `api-client.ts` no longer hand-copies `Be*` wire types — it imports the real schemas from `@post-anki/shared` (`import type * as be`) and the mappers are typechecked against them. `@post-anki/shared` added to web deps (symlinked workspace). A BE schema field rename now BREAKS the web build instead of silently passing. This closes the "types could diverge at runtime" risk; the only divergence the FE still maps on purpose is its decoupled VIEW model (depth `aware`↔`awareness`, gap `status`↔`state`, vestigial `socratic`) — those mappings are now checked against shared on both sides.
- [x] RUNTIME VERIFIED (2026-06-01): a full `apps/api/.env` already existed (real Neon `post-anki` DB `cool-night-49934898` + real OpenRouter key + `API_SHARED_SECRET=local-e2e-secret`). Booted the real API and ran BOTH harnesses against real DB + real LLM: (a) `apps/api/scripts/verify-e2e.ts` — full create→AI-parse→ready→confirm→probe→answer→daily-push loop PASSED; (b) `apps/web/scripts/verify-api-client.ts` (new, mirrors the BE harness) — every FE api-client mapper PASSED parsing live responses (subjects, curricula status-enum map, detail module→topic + depth map, `GET /topics/:id/gaps` status map + computed gapsTotal, daily-push, cross-cutting). Real HTTP, real bearer auth, real 201/202/200. "Full compatibility with ready endpoints" now a FACT, not an assumption.
- [ ] LOCAL-DEV GOTCHA (this machine): node-postgres to Neon timed out (`connect ETIMEDOUT …:5432`) until the API was started with `NODE_OPTIONS="--dns-result-order=ipv4first --no-network-family-autoselection"` — this Mac has no IPv6 route to AWS and Node's Happy-Eyeballs mishandles the IPv6 EHOSTUNREACH → IPv4 fallback. `nc` to the pooler:5432 succeeds, so it's purely Node's family-autoselect. Consider baking the flag into `apps/api` dev script or documenting it. May be environment-specific.
- [ ] Test data left in the dev DB from the e2e runs (subjects "E2E Backend"/"Runtime Verify", curriculum "Fundamentals of Back-End…"). Harmless; left in place (didn't run destructive SQL autonomously).
- [ ] PARTIAL RESPONSE (BE inconsistency, BE-side fix): `POST /curricula/:id/sources` and `/reparse` return `{ id, name, status }`, not a full `Curriculum` (every other curriculum endpoint returns full). Harmless today (FE refetches), but worth making BE return full Curriculum for consistency.
- [ ] DEPTH ceiling on discovered gaps / `concern` declaration: FE `declareGapInput`/`curateGapInput` don't expose `concern` (BE supports it optionally). Not an endpoint gap — a future enrichment so user-declared gaps can carry a concern tag that rolls up in `/concerns`.

## api-curriculum-slice (2026-05-31)
- [x] Probing session — BUILT (`POST /topics/:id/probe[/answer]`, mentor-ask + mentor-eval agents, gated behind `confirmed`).
- [x] Gap declare/curate — BUILT (`POST /gaps`, `PATCH /gaps/:id`).
- [x] Daily push selection — BUILT (`GET /daily-push`). [x] /decide — BUILT (`POST /decide`). [x] Cross-cutting — BUILT (`GET /cross-cutting`).
- [ ] `CurriculumDetail` does not yet expose gaps per topic — add when the probing UI needs the full gap list inline.
- [ ] "Update a curriculum" with a new source = append/merge new modules+topics (decided), but only initial create parses today — merge/dedupe endpoint not built. Also no re-parse endpoint after `failed`.
- [ ] Mentor does not yet conversationally *ask* "how deep do you want to go" during creation; depth is set per-topic via PATCH + architect default. The conversational asking step is unbuilt.
- [ ] Daily-push cron trigger is infra (Cloud Scheduler → `GET /daily-push`), not built. Adaptive speed/hinting (INBOX) not modeled.

## adaptive loop / contract reconciliation (2026-05-31)
- [ ] INTEGRATION: web wires off a local mock model (`apps/web/src/curriculum/model.ts`), decoupled from `@post-anki/api` because the other engineer is authoring that contract + endpoints in parallel. When their endpoints land, replace `apps/web/src/stub/store.ts` with real `@post-anki/api` calls and delete the local model. The local progress shape already matches their `progress.ts`; reconcile Module/Question-kind/attempt shapes.
- [ ] Confirm the hierarchy with the engineer: web assumes Curriculum → Module → Topic → Question, question `kind` = socratic|quick_test, progress = maturity 0–100 + status. Their contract must agree (their `progress.ts` already does).
- [ ] Maturity math (pass +25 / fail −15, module = avg maturity) is a stub heuristic — the real adaptive/depth-calibration logic is the engineer's + Mastra's to define.

## web UI + monorepo (2026-05-31)
- [ ] TERMINOLOGY: reconcile the provisional Subject→Curriculum→Topic hierarchy with the existing Tool→Concept hierarchy. Does a Curriculum replace a Tool? Is a Topic a Concept? Is a Subject new above Tool? (deferred during UI draft; see GLOSSARY provisional section).
- [ ] The curriculum builder IS the Phase-1 "distillation" feature (backlog #35 / discussion.md "consult docs → break into modules/topics → probe") — confirm and link them so they aren't built twice.
- [ ] BOT DEPS BROKEN: `@mastra/memory`/`@mastra/pg` pinned at `^0.20.0` but no `0.20.x` was ever published (Mastra reset to `1.x`; `@mastra/core` does have `0.20.2`). Bot can't `npm install`. Upgrade all `@mastra/*` to compatible `1.x` (and adjust bot code for API changes) — until then `apps/bot` is excluded from the root `workspaces` array.
- [ ] Bot relocated to `apps/bot` with a standalone Dockerfile. `deploy.yml` was NOT updated and still does `docker build .` (no root Dockerfile now) + `npm ci` at root (pulls the broken bot). Deferred since the bot deploy is blocked anyway — fix `deploy.yml` (build `-f apps/bot/Dockerfile apps/bot`, scope the test gate) as part of the bot rejoin.
- [ ] CI `deploy.yml` test job runs `npm run typecheck`/`npm test` at root → fans out across all workspaces (web/api too). Confirm that's desired for the bot's deploy gate, or scope it to `-w @post-anki/bot`.
- [ ] `apps/web` BFF currently returns in-memory mock; decide when to flip `stub/store.ts` to real HTTP calls against `apps/api` (needs `API_BASE_URL` env + the api engineer's endpoints live).

## initial-mastra-telegram-slice / preflight.md
- [ ] G1 — Exact OpenRouter model id for slice 1 (default `openrouter/google/gemini-2.5-flash`, swap freely) — affects cost/quality but not architecture.
- [ ] G2 — GCP project id and Artifact Registry repo name — required before first deploy.
- [ ] G3 — Telegram bot token does not exist yet — must be created via @BotFather before deploy.

## deployment IaC (bedtime-agent port)
- [x] RESOLVED — single project `post-anki` (billing 5-project cap forced collapse from bootstrap+prod). State bucket `gs://post-anki-pulumi-state` lives in `post-anki`. WIF + CI SA provisioned. See `docs/ci-cd/gcp-setup.md`.
- [x] RESOLVED (G2) — GCP project = `post-anki`, Artifact Registry repo = `post-anki` (created by Pulumi on first `up`).
- [ ] Confirm `ilya.online` DNS is self-managed (Route53?) so a `postanki` CNAME → `ghs.googlehosted.com` can be added after first `pulumi up`.
- [ ] PREREQUISITE before first deploy: no Drizzle migration files exist yet (`src/db/migrations` absent) — run `npm install` + `npm run db:generate` and commit, else the bot 500s on first message (no `study_profile` table) and the Docker build fails on the missing migrations dir.
- [ ] Future `post-anki-dev` cloud env needs a freed billing slot or a quota increase (account at 5/5).

## initial-mastra-telegram-slice / discussion.md
- [ ] After slice 1 lives in production, decide the shape of "study index" persistence: hardcoded → YAML in repo → Mastra `workingMemory` with LibSQL on a persistent volume → Cloud SQL. User described a future where "if I choose to start learning Next.js, the system should consult the docs and break things down into modules and topics and probe me for a particular piece of knowledge" — this is the Phase 1 distillation feature (backlog #35), not slice 1.
- [ ] After slice 1, decide on Google Secret Manager vs continuing to pass GitHub Actions secrets straight into Cloud Run env vars. Triggered when more than 2–3 secrets accumulate or when a rotation cadence is needed.
- [ ] Decide whether `setWebhook` bootstrap should remain a manual one-shot or move into a `workflow_dispatch` GH Action. Manual is safer (you only run it on token change); automated is more discoverable.
- [ ] Should Cloud Run timeout be lowered from default 60s to something tighter (e.g. 30s) once we know LLM latency profile? Affects retries and cost ceiling.
